<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta
            name="viewport"
            content="width=<device-width>, initial-scale=1.0"
        />
        <%- include('../partials/head'); %>
    </head>
    <body>
        <header><%- include('../partials/header'); %></header>
        <p>Edit</p>
        <form action="/users/<%= data.id %>?_method=PUT" method="post">
            <label
                >ID
                <input
                    type="hidden"
                    name="id"
                    value="<%= locals.data.id %>"
                />
            </label>
            <label
                >Name
                <input
                    type="text"
                    name="name"
                    value="<%= locals.data.name %>"
                />
            </label>
            <br/>
            <label>Email
                <input
                    type="text"
                    name="email1"
                    value="<%= locals.data.email1 %>"
                />
            </label>
            <button type="submit">Submit</button>
        </form>
    </body>
</html>
